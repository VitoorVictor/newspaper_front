import type { ReactNode } from "react";

export const metadata = {
  title: "Trabalhe Conosco",
  description: "Trabalhe Conosco na Revista Imagem Industrial.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
