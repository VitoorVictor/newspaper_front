import type { ReactNode } from "react";

export const metadata = {
  title: "Sobre",
  description: "Sobre a Revista Imagem Industrial e nosso projeto.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
