import type { ReactNode } from "react";

export const metadata = {
  title: "Coluna Empresarial",
  description: "Vejas algumas empresas com expressão na região.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
