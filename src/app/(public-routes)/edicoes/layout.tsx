import type { ReactNode } from "react";

export const metadata = {
  title: "Edições",
  description: "Vejas as edições da Revista Imagem Industrial.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
