import type { ReactNode } from "react";

export const metadata = {
  title: "Anuncie",
  description: "Anuncie na Revista Imagem Industrial.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
