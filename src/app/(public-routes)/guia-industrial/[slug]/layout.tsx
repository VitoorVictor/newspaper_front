import type { ReactNode } from "react";

export const metadata = {
  title: "Guia Industrial",
  description: "Veja a Industria ou Representante na integra.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
