import type { ReactNode } from "react";

export const metadata = {
  title: "Assinar Revista",
  description: "Assine a Revista Imagem Industrial.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
